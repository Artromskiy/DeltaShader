name: Build, tests and benchmarks

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      run_benchmarks:
        description: Run registered BenchmarkDotNet projects
        required: true
        default: false
        type: boolean
      filter:
        description: BenchmarkDotNet filter
        required: true
        default: "*"
        type: string
      warmup_count:
        required: true
        default: 3
        type: number
      iteration_count:
        required: true
        default: 5
        type: number
      launch_count:
        required: true
        default: 1
        type: number

permissions:
  contents: read

concurrency:
  group: delta-shader-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  DOTNET_CLI_TELEMETRY_OPTOUT: 1
  DOTNET_NOLOGO: 1
  NUGET_PACKAGES: ${{ github.workspace }}/.nuget/packages

jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout Delta.Shader
        uses: actions/checkout@v7
        with:
          path: Delta.Shader
      - name: Checkout Delta.Maths
        uses: actions/checkout@v7
        with:
          repository: Artromskiy/DVG.Maths
          path: Maths
      - name: Checkout DeltaRender
        uses: actions/checkout@v7
        with:
          repository: Artromskiy/DeltaRender
          path: DeltaRender
      - name: Install .NET 10
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x
      - name: Cache NuGet packages
        uses: actions/cache@v6
        with:
          path: ${{ env.NUGET_PACKAGES }}
          key: delta-shader-nuget-${{ runner.os }}-${{ hashFiles('Delta.Shader/**/*.csproj', 'Maths/**/*.csproj', 'DeltaRender/**/*.csproj') }}
          restore-keys: delta-shader-nuget-${{ runner.os }}-
      - name: Install Vulkan and SPIR-V tools
        run: sudo apt-get update && sudo apt-get install -y glslang-tools spirv-tools mesa-vulkan-drivers
      - name: Restore and build
        working-directory: Delta.Shader
        run: |
          dotnet restore Delta.Shader.sln
          dotnet build Delta.Shader.sln -c Release --no-restore
      - name: Run tests
        working-directory: Delta.Shader
        run: dotnet test Delta.Shader.sln -c Release --no-build --no-restore
      - name: Benchmark discovery smoke
        working-directory: Delta.Shader
        shell: bash
        run: |
          mapfile -t projects < <(find . -type f -name '*.Benchmarks.csproj' | sort)
          for project in "${projects[@]}"; do
            dotnet run --project "$project" -c Release -- --list flat --filter '*'
          done

  benchmarks:
    if: github.event_name == 'workflow_dispatch' && inputs.run_benchmarks
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    steps:
      - name: Checkout Delta.Shader
        uses: actions/checkout@v7
        with:
          path: Delta.Shader
      - name: Checkout Delta.Maths
        uses: actions/checkout@v7
        with:
          repository: Artromskiy/DVG.Maths
          path: Maths
      - name: Checkout DeltaRender
        uses: actions/checkout@v7
        with:
          repository: Artromskiy/DeltaRender
          path: DeltaRender
      - name: Install .NET 10
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x
      - name: Run registered benchmarks
        working-directory: Delta.Shader
        shell: bash
        run: |
          mkdir -p artifacts/benchmarks
          mapfile -t projects < <(find . -type f -name '*.Benchmarks.csproj' | sort)
          if [ ${#projects[@]} -eq 0 ]; then
            echo 'No *.Benchmarks.csproj is registered.' >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi
          for project in "${projects[@]}"; do
            dotnet run --project "$project" -c Release -- \
              --filter '${{ inputs.filter }}' \
              --warmupCount '${{ inputs.warmup_count }}' \
              --iterationCount '${{ inputs.iteration_count }}' \
              --launchCount '${{ inputs.launch_count }}' \
              --exporters json csv markdown github \
              --artifacts artifacts/benchmarks
          done
      - name: Upload benchmark artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: delta-shader-benchmarks-${{ github.run_number }}
          path: Delta.Shader/artifacts/benchmarks
          if-no-files-found: warn
          retention-days: 30
